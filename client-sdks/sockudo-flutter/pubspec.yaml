name: sockudo_flutter
description: "Sockudo client port for Flutter and Dart."
version: 1.0.0
homepage: https://github.com/sockudo/sockudo-flutter
repository: https://github.com/sockudo/sockudo-flutter
issue_tracker: https://github.com/sockudo/sockudo-flutter/issues
topics:
  - websocket
  - realtime
  - realtime-client
  - sockudo
  - flutter

environment:
  sdk: ^3.11.3
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.1
  crypto: ^3.0.6
  http: ^1.5.0
  fixnum: ^1.1.1
  messagepack: ^0.2.1
  meta: ^1.17.0
  protobuf: ^6.0.0
  sodium: ^3.4.5
  sodium_libs: ^3.4.5
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
