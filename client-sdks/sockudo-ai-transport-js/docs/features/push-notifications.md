# Push Notifications

Sockudo push rules can turn an agent completion publish into a channel-targeted push notification.
Push payloads should carry a session id, then the app should load authoritative history after open.
