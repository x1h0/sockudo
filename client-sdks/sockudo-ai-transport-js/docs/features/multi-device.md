# Multi-Device Sessions

All tabs and devices subscribed to the same session channel fold the same serial-ordered operations.
Use `useMessageSync` to keep Vercel `useChat` overlays aligned with Sockudo's authoritative view.
