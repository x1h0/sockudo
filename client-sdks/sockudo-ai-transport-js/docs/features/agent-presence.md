# Agent Presence

Presence data can expose `idle`, `thinking`, `streaming`, and suspended states. Use presence updates
rather than leave/re-enter flaps when the server supports `sockudo:presence_update`.
