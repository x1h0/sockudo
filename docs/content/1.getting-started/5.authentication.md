---
title: Authentication
description: Private/presence channel authorization and user signin flow.
navigation:
  icon: i-lucide-shield-check
---

## Channel Authorization (Private/Presence)

For `private-*` and `presence-*` channels, Sockudo validates the same HMAC-SHA256 signature format:

```text
auth = "<app-key>:HMAC_SHA256(socket_id:channel_name[:channel_data], app_secret)"
```

- Private channel: sign `socket_id:channel_name`
- Presence channel: sign `socket_id:channel_name:channel_data`

`channel_data` must contain at least:

```json
{
  "user_id": "user-123",
  "user_info": { "name": "Alice" }
}
```

The auth signature format is identical regardless of protocol version (V1 or V2).

## Auth Endpoint (Sockudo Server SDK)

```js
const { Sockudo } = require("sockudo");
const express = require("express");

const app = express();
app.use(express.json());

const sockudo = new Sockudo({
  appId: "app-id",
  key: "app-key",
  secret: "app-secret",
  host: "127.0.0.1",
  port: 6001,
  useTLS: false,
});

app.post("/sockudo/auth", (req, res) => {
  const { socket_id, channel_name } = req.body;

  if (channel_name.startsWith("presence-")) {
    const presenceData = {
      user_id: "user-123",
      user_info: { name: "Alice" },
    };
    return res.send(sockudo.authorizeChannel(socket_id, channel_name, presenceData));
  }

  return res.send(sockudo.authorizeChannel(socket_id, channel_name));
});
```

## Client Configuration

::code-group

```js [V2 (Sockudo)]
import { SockudoClient } from "@sockudo/client";

const client = new SockudoClient("app-key", {
  wsHost: "127.0.0.1",
  wsPort: 6001,
  forceTLS: false,
  channelAuthorization: {
    endpoint: "http://127.0.0.1:3000/sockudo/auth",
    transport: "fetch",
  },
});
```

```js [V1 (Pusher)]
import Pusher from "pusher-js";

const pusher = new Pusher("app-key", {
  wsHost: "127.0.0.1",
  wsPort: 6001,
  cluster: "",
  forceTLS: false,
  authEndpoint: "http://127.0.0.1:3000/sockudo/auth",
});
```

::

## User Signin (`pusher:signin` / `sockudo:signin`)

If `policy.features.enable_user_authentication` is enabled on the app, client-side user signin uses `pusher:signin` (V1) or `sockudo:signin` (V2). The signature format is the same regardless of protocol version:

```text
auth = "<app-key>:HMAC_SHA256(socket_id::user::user_data, app_secret)"
```

```js
client.signin();
```

Configure the endpoint via `userAuthentication.endpoint` (default `/sockudo/user-auth`).

## Common Failures

- Missing `auth` for private/presence subscription.
- Invalid JSON in `channel_data`.
- Missing `user_id` in presence `channel_data`.
- Clock skew on signed HTTP API requests (more than 10 minutes).
