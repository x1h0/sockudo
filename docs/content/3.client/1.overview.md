---
title: Overview
description: Official Sockudo clients for JavaScript, Swift, Kotlin, and Flutter.
navigation:
  icon: i-lucide-circle-gauge
---

Sockudo ships multiple official clients:

- `@sockudo/client` for browser, Node.js, workers, and React Native
- `sockudo-swift` for iOS, macOS, tvOS, and watchOS
- `sockudo-kotlin` for Android and JVM applications
- `sockudo_flutter` for Flutter and pure Dart applications

All official clients preserve the Pusher mental model while adding first-class support for Sockudo-specific capabilities.

## What Stays Compatible

- `new Pusher(appKey, options)`
- `subscribe`, `unsubscribe`
- private/presence auth flow
- channel `bind`/`unbind`
- client events (`client-*`)

## What Gets Added

- filter-aware subscribe options.
- delta compression negotiation + decoding.
- per-channel delta settings.
- filter helper exports.

## Runtime Targets

- Browser
- Node.js
- Web Worker
- React Native
- iOS, macOS, tvOS, watchOS
- Android and other JVM runtimes
- Flutter mobile, desktop, and Dart

## Shared Advanced Features

- filter-aware subscription options
- delta compression negotiation and reconstruction
- encrypted channel auth and payload decryption
- private, presence, and encrypted channels
- user sign-in and watchlist event handling
- live integration coverage against Sockudo on port `6001`

Use this when you want Sockudo advanced delivery features without building custom protocol handlers.
