---
title: Official Client Features
description: Tag filtering and delta compression workflows across official Sockudo clients.
navigation:
  icon: i-lucide-wand-sparkles
---

## Features In Official Sockudo Clients

- Filter-aware subscription API.
- Automatic `pusher:delta` decoding.
- Per-subscription delta negotiation.
- Delta stats hooks and error hooks.
- Encrypted channel shared-secret auth and payload decryption.

## Tag Filtering Subscribe API

```ts
import Pusher from "@sockudo/client";
import { Filter } from "@sockudo/client/filter";

const pusher = new Pusher("app-key", {
  cluster: "mt1",
  wsHost: "127.0.0.1",
  wsPort: 6001,
  forceTLS: false,
});

const channel = pusher.subscribe(
  "market-data",
  Filter.and(
    Filter.eq("event_type", "trade"),
    Filter.gte("price", "100")
  )
);
```

```swift
let channel = client.subscribe(
    "market-data",
    options: .init(
        filter: .and([
            .eq("event_type", "trade"),
            .gte("price", "100")
        ])
    )
)
```

```kotlin
val channel =
    client.subscribe(
        "market-data",
        SubscriptionOptions(
            filter =
                Filter.and(
                    Filter.eq("event_type", "trade"),
                    Filter.gte("price", "100"),
                ),
        ),
    )
```

```dart
final channel = client.subscribe(
  'market-data',
  options: SubscriptionOptions(
    filter: Filter.and(<FilterNode>[
      Filter.eq('event_type', 'trade'),
      Filter.gte('price', '100'),
    ]),
  ),
);
```

## Delta Compression (Global Enable)

```ts
const pusher = new Pusher("app-key", {
  cluster: "mt1",
  wsHost: "127.0.0.1",
  wsPort: 6001,
  forceTLS: false,
  deltaCompression: {
    enabled: true,
    algorithms: ["fossil", "xdelta3"],
    debug: false,
    onStats: (stats) => console.log(stats.bandwidthSavedPercent),
  },
});
```

```swift
let client = try SockudoClient(
    "app-key",
    options: .init(
        cluster: "local",
        forceTLS: false,
        wsHost: "127.0.0.1",
        wsPort: 6001,
        deltaCompression: .init(
            enabled: true,
            algorithms: [.fossil, .xdelta3],
            debug: false
        )
    )
)
```

```kotlin
val client =
    SockudoClient(
        "app-key",
        SockudoOptions(
            cluster = "local",
            forceTls = false,
            wsHost = "127.0.0.1",
            wsPort = 6001,
            deltaCompression =
                DeltaOptions(
                    enabled = true,
                    algorithms = listOf(DeltaAlgorithm.fossil, DeltaAlgorithm.xdelta3),
                    debug = false,
                ),
        ),
    )
```

```dart
final client = SockudoClient(
  'app-key',
  const SockudoOptions(
    cluster: 'local',
    forceTls: false,
    wsHost: '127.0.0.1',
    wsPort: 6001,
    deltaCompression: DeltaOptions(
      enabled: true,
      algorithms: <DeltaAlgorithm>[
        DeltaAlgorithm.fossil,
        DeltaAlgorithm.xdelta3,
      ],
      debug: false,
    ),
  ),
);
```

## Per-Subscription Delta Options

```ts
// delta with algorithm
pusher.subscribe("ticker:BTC", {
  delta: { enabled: true, algorithm: "fossil" },
});

// filter + delta together
pusher.subscribe("events", {
  filter: Filter.eq("priority", "high"),
  delta: { algorithm: "xdelta3" },
});
```

```swift
let channel = client.subscribe(
    "events",
    options: .init(
        filter: .eq("priority", "high"),
        delta: .init(algorithm: .xdelta3)
    )
)
```

```kotlin
val channel =
    client.subscribe(
        "events",
        SubscriptionOptions(
            filter = Filter.eq("priority", "high"),
            delta = ChannelDeltaSettings(algorithm = DeltaAlgorithm.xdelta3),
        ),
    )
```

```dart
final channel = client.subscribe(
  'events',
  options: const SubscriptionOptions(
    filter: FilterNode(key: 'priority', cmp: 'eq', val: 'high'),
    delta: ChannelDeltaSettings(algorithm: DeltaAlgorithm.xdelta3),
  ),
);
```

## Conflation Key Support

`@sockudo/client` automatically handles conflation keys when the server enables them. The client:

- Receives `pusher:delta_cache_sync` messages with entity-grouped states
- Maintains separate delta histories per conflation key (e.g., per stock symbol, per device ID)
- Reconstructs delta messages using the correct entity's base message
- Tracks cache stats per conflation key when `debug: true`

**No additional client configuration needed** — conflation is configured server-side and the client adapts automatically.

**Example server config:**
```json
{
  "channel_delta_compression": {
    "market:*": {
      "enabled": true,
      "conflation_key": "symbol",
      "max_messages_per_key": 100,
      "max_conflation_keys": 1000
    }
  }
}
```

When you subscribe to `market:live`, the client will automatically group BTC, ETH, and other symbols' delta states independently.

See [Delta Compression](/server/delta-compression#conflation-keys) for server configuration details.

## Client Coverage

| Feature | JS/TS | Swift | Kotlin | Flutter |
|---|---|---|---|---|
| Filters | Yes | Yes | Yes | Yes |
| Fossil delta | Yes | Yes | Yes | Yes |
| Xdelta3 / VCDIFF | Yes | Yes | Yes | Yes |
| Encrypted channels | Yes | Yes | Yes | Yes |
| Private / presence auth | Yes | Yes | Yes | Yes |
| User sign-in | Yes | Yes | Yes | Yes |
| Watchlist events | Yes | Yes | Yes | Yes |
| Live integration tests | Yes | Yes | Yes | Yes |

## Plain Pusher Clients

Core channels/events still work on plain pusher-js and Echo.  
They just do not expose this advanced API surface out of the box.
