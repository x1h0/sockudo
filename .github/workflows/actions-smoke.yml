name: Actions Smoke

on:
  workflow_dispatch: {}
  repository_dispatch:
    types: [actions-smoke]
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  smoke:
    name: Smoke
    runs-on: ubuntu-24.04
    steps:
      - name: Verify Actions scheduler
        run: echo "actions scheduler is accepting runs"
